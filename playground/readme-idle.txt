You will need to run 'python start.py' in order to execute the IDLE from this
directory, not the one from the python installation.

Also, a Python of 2.6 or greater is required, because the sys.getsizof feature
is used.
