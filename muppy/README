Note that if muppy is used with CPython 2.6 or higher, it will use sys.getsizeof
to retrieve the size of an object. If an earlier Python version is used, the
asizeof module by Jean Brouwers will be used.
